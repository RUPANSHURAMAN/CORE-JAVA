package exetndsimplements;

//in java classes doesn't support multiple inheritance but then interfaces supports multiple inheritance 
public interface A {
// interface can't keep anything complete not even constructor as object create like abstract but 
	// abstract can have constructor
	public void test1();
}
